name: Verify TENNIS full (6 books)

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/verify-tennis-full.js'
      - '.github/workflows/verify-tennis-full.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: arbitrage-service
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: arbitrage-service/node_modules
          key: npm-${{ hashFiles('arbitrage-service/package.json') }}
      - run: npm install --production
      - working-directory: .
        run: ln -sf arbitrage-service/node_modules node_modules
      - name: Run verify full
        working-directory: .
        run: |
          mkdir -p probe-results
          node scripts/verify-tennis-full.js 2>&1 | tee probe-results/verify-tennis-full.log || true
        env:
          PROXY_MODE: ${{ secrets.PROXY_MODE }}
          JINA_API_KEY: ${{ secrets.JINA_API_KEY }}
          RESIDENTIAL_PROXY_URL: ${{ secrets.RESIDENTIAL_PROXY_URL }}
          CF_WORKER_PROXY_URL: ${{ secrets.CF_WORKER_PROXY_URL }}
      - name: Commit log
        if: always()
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [ -f probe-results/verify-tennis-full.log ]; then
            git add probe-results/verify-tennis-full.log
            git commit -m "VERIFY tennis full ${{ github.run_id }}" || echo "nothing"
            git push || true
          fi
