name: Probe SportyBet Tennis — Cartographie markets
on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: arbitrage-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Probe SportyBet tennis mapping
        run: node scripts/probe-sb-tennis-map.js
