name: Debug BetPawa+YellowBet+1win Tennis structures
on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    defaults:
      run:
        working-directory: arbitrage-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install deps
        run: npm install --no-save ws got-scraping
      - name: Debug structures
        run: node scripts/debug-bp-yb-tennis.js
